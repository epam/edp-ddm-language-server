apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ddm-language-server.fullname" . }}-test-connection"
  labels:
    {{- include "ddm-language-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ddm-language-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
