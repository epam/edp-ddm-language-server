spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        enable-api: false
        paths:
          - /app/config/application.yml
---
spring:
  config:
    activate:
      on-profile: local
  cloud:
    kubernetes:
      config:
        enabled: false